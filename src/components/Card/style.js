import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    card: {
        width: '60%',
        borderRadius: 5,
        borderWidth: 1,
        borderColor: '#939393',
        padding: 10,
        backgroundColor: '#FFF'
    },
    cardContainer: {
        alignItems: 'center',
        width: '100%',
        marginTop: 20
    },
    cardContent: {
        marginTop: 20,
    },
});

export default style;